{
  "name": "myglambeauty-supply",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:generate": "pnpm --filter @myglambeauty/db generate",
    "db:push": "pnpm --filter @myglambeauty/db push",
    "db:seed": "pnpm --filter @myglambeauty/db seed"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18"
  }
}
